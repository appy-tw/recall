'use strict';

/**
 * @ngdoc function
 * @name recallApp.controller:CheckinCtrl
 * @description
 * # CheckinCtrl
 * Controller of the recallApp
 */
angular.module('recallApp')
  .controller('CheckinCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
